import { StackSplitPipe } from './stack-split.pipe';

describe('StackSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new StackSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
